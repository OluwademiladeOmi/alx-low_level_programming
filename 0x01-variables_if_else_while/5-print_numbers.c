#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 *main-program entry point.
 *
 *Return:0 no error, non zero if error
 */

int main(void)
{

		int i;

		for (i = 0; i < 10; i++)
		{
			printf("%d", i);
		}
			printf("\n");
			return (0);
}
